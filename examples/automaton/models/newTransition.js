var Automata;
(function (Automata) {
    var Models;
    (function (Models) {
        class NewTransition extends Models.Transition {
        }
        Models.NewTransition = NewTransition;
    })(Models = Automata.Models || (Automata.Models = {}));
})(Automata || (Automata = {}));
